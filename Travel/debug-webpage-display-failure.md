[OPEN] webpage-display-failure

# Debug Session

- Session ID: webpage-display-failure
- Started At: 2026-04-04
- Scope: 网页无法正常显示，排查当前首页和相关页面的阻塞原因

## Symptoms

- 页面无法正常显示
- 需要确认是编译错误、运行时异常、断链路由还是资源缺失

## Hypotheses

1. 最近手动删除了一批文件后，页面仍保留对这些模块的引用，导致编译失败。
2. 当前开发服务器仍在使用旧缓存或旧进程日志，导致恢复后的代码没有被正确重新编译。
3. 侧边栏或概览页还存在指向已删除页面的入口，触发后表现为“网页无法正常显示”。
4. API 路由或首页依赖的辅助模块缺失，导致主页面渲染链路在服务端就中断。
5. 当前页面已经能编译，但浏览器端还有新的运行时报错阻止内容显示。

## Evidence

- 待收集。
