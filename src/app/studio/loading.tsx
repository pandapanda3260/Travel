import { RouteLoadingShell } from "../_components/route-loading-shell";

export default function StudioLoading() {
  return (
    <RouteLoadingShell
      pageName="Studio"
      title="创作工作台加载中..."
      description="正在进入内容创作页面并准备工作流数据。"
    />
  );
}
